# HSSA 复现报告(仿真)

复现对象:Li et al., *Opt. Lett.* 50, 3257 (2025) 及其 Supplement 1。论文未公开代码与数据,因此本复现用物理仿真生成数据,按论文描述实现算法,再与仿真真值对照。所有数值见 [`results/summary.md`](results/summary.md),原始结果在 `results/*.json`。

## 1. 结论摘要

- **能复现的部分。** 按 Algorithm 1 实现的 HSSA 能工作,并超过传感器奈奎斯特极限:1.34 µm 像素对应的极限约为 8-4(线宽 1.38 µm),而 3 高度 × 1 角度在三个随机种子上都稳定分辨到 9-1(0.98 µm)。高度数增加时误差明显下降,这与论文 Fig. 2 的趋势一致。LISA(单高度 × 9 角度)在已知照明角时能分辨到 9-2,但单一高度留下明显孪生像,MSE 约为 MFAP 的 4 倍,这与论文"LISA 伪影严重"的描述一致。
- **没有复现的部分。** 论文的核心结论是"3 高度 × 3 角度的 HSSA 优于 9 高度的 MFAP"。本仿真中结论相反:9 帧 MFAP 的误差约为 HSSA 的一半,分辨率也更高。在 3 个高度下再加斜照明角度,误差不降反升。
- **原因已定位。** HSSA 用正入射角谱核加横向平移来描述斜照明帧,忽略了斜照明的非傍轴离焦(沿倾斜方向等效距离 z/cos³θ,垂直方向 z/cosθ)。在 8°、1.4 mm 时两方向相差约 28 µm,是焦深的数倍。把斜照明帧换成等量纯横移,HSSA 立即与 MFAP 持平。
- **照明分离变量 p 基本不动。** 按论文参数 γ = 0.05,p 与 1 的平均偏差只有 0.26 %,关闭 p 更新对结果没有可测影响。
- **一个不需要标定的修正。** 用同一高度上斜照明帧相对正入射帧的配准位移反推倾角,改用倾斜谱核,HSSA 的误差回到 MFAP 的水平,三个种子都分辨到 9-2。这保留了论文"无需照明角先验"的优点。
- **仿真无法回答的问题。** 论文实验中 HSSA 的优势可能来自实验层面:HSSA 只需 3 次手动轴向移动,MFAP 需要 9 次。本仿真的轴向位置是精确的,只加了 2 µm 横向抖动,没有模拟手动平台的轴向误差和倾斜。要判断这一点需要真实数据。

## 2. 复现设置

### 2.1 来自论文与补充材料的参数

| 项目 | 数值 | 出处 |
|---|---|---|
| 波长 | 532 nm | 正文 Fig. 1 |
| 像素尺寸 | 1.34 µm | 正文 |
| 针孔–样品距离 L | 约 70 cm | 正文、Supplement Note 1 |
| 针孔最大横移 D | 约 10 cm,即照明角 ≤ 8°,随机选取 | 正文、Supplement Note 1 |
| 最近样品–传感器距离 | 0.975 mm | Supplement Note 1 |
| 帧数 | 3 高度 × 3 角度 = 9;对比方法同为 9 帧 | 正文 |
| HSSA 参数 | α = 0.2,β = 0.7,γ = 0.05,T = 50 | Algorithm 1 |
| 初始化 | φ0 = x0 = √I1,p0 = 1 | Algorithm 1 |
| 预处理 | 上采样、ADFrFT 自动对焦、回传到样品面后做傅里叶互相关配准 | 正文 Fig. 1(b) |
| 自动对焦 | ADFrFT,粗搜 Δp = 0.2、精搜 Δp = 0.8 | 自动对焦论文 Opt. Lasers Eng. 175, 107991 |

### 2.2 论文未给出、本复现自行选定的内容

| 项目 | 选择 | 说明 |
|---|---|---|
| 上采样倍数 | 2(重建像素 0.67 µm) | 推测值:Supplement Note 4 报告全幅 9 帧 50 次迭代在笔记本 GPU 上约 105 s,4 倍上采样的显存和耗时都难以做到 |
| HSSA 的 3 个高度 | 0.975 / 1.175 / 1.375 mm | 论文只说 d_n < 2 mm |
| MFAP 的 9 个高度 | 0.975–1.375 mm,步长 50 µm | 与 HSSA 覆盖相同轴向范围 |
| 传感器窗口 | 512 × 512 像素(686 µm) | 全幅仿真计算量过大;窗口足够大,不限制 8-6 的分辨 |
| 像素约束 | 默认按像素积分(先平方再分箱)投影;另测"上采样后直接替换模值"的字面读法 | 两种读法都与 Fig. 1(b) 相容,前者物理上正确,实测也更好 |
| MFAP | 与 HSSA 相同的并行平均投影,不做 p 分离 | 论文引用的 MFAP 实现细节未给出 |
| LISA | 已知照明角,用倾斜谱核 H(f + f0);9 帧共用正入射帧的自动对焦距离 | 论文说 LISA 依赖精确的角度先验 |
| ADFrFT 的输入 | 回传场的强度 |U|² | 用复振幅或振幅时对焦曲线在 975 µm 处没有峰;用强度时三种测试帧都对准 |

### 2.3 仿真比重建模型更"真实"

为避免同一模型既生成数据又重建("inverse crime"),仿真包含重建模型没有的因素:

- 针孔发出的球面波,按精确角谱法传播斜照明(包含非傍轴项);
- 0.335 µm 细网格,再按 4 × 4 像素积分到传感器;
- 每个高度 2 µm 的随机横向平台抖动;
- 平滑的背景照明不均匀(振幅 ±5 %,相位 ±0.3 rad,相关长度 150 µm),即 p 应当吸收的部分;
- 泊松散粒噪声(背景 2000 e⁻)、2 e⁻ 读出噪声、10 bit 量化。

另有一组"理想化"仿真(平面波、无背景,保留噪声),用于确认结论不依赖这些非理想因素。

### 2.4 评价方式

- **MSE**:重建振幅按清晰玻璃区归一化到 1,对齐真值后,在 USAF 第 7–9 组所在的中心区域(论文用的也是中心区域)计算。论文用二值化的重建图作真值,本复现用的是仿真真值,因此 MSE 的绝对数值不能直接和论文比,只比较趋势和方法间的相对关系。
- **最细可分辨单元**:第 7–9 组内,线对间隙相对相邻线条的 Michelson 对比度在两个方向都 ≥ 0.1,且所有更粗单元都满足。真值本身在 0.67 µm 网格上最多到 9-3,所以 9-2 基本是本设置的上限。
- 真实化仿真跑了 3 个随机种子(照明角、抖动、噪声、背景都不同),下表给出均值 ± 标准差。

## 3. 结果

### 3.1 对应论文 Fig. 2:高度数 × 角度数

真实化仿真,3 个种子的 MSE 均值(括号内为各种子的最细可分辨单元):

| 高度数 \ 角度数 | 1 | 2 | 3 |
|---|---|---|---|
| 1 | 0.0448(无,无,无) | 0.0340(7-2,无,8-1) | 0.0345(7-6,7-2,无) |
| 2 | 0.0160(8-6,8-6,8-6) | 0.0144(8-3,8-3,8-1) | 0.0140(8-3,8-2,8-1) |
| 3 | 0.0091(9-1,9-1,9-1) | 0.0111(8-6,8-4,8-5) | 0.0115(8-4,8-2,8-1) |

与论文对照:

- 高度数增加,误差明显下降(论文 0.0978 → 0.0443 → 0.0226,本复现 0.0448 → 0.0160 → 0.0091,均为 1 个角度)。趋势一致。
- 论文中角度增加也让误差下降。本复现只在 1 个高度时成立;2 个高度时略降,3 个高度时反而上升。
- 论文称 3 高度 × 2 角度是首次分辨 8-6 的最少配置。本复现中 3 × 2 在三个种子上分别到 8-6、8-4、8-5,而 3 × 1 已稳定到 9-1。

图:[`results/fig2_usaf_grid.png`](results/fig2_usaf_grid.png)

### 3.2 对应论文 Fig. 3:每种方法 9 帧

| 方法 | MSE(3 种子) | 最细可分辨单元 | 论文结果 |
|---|---|---|---|
| 单帧回传(参考) | 0.0378 ± 0.0006 | 无 | — |
| LISA,1 高度 × 9 角度 | 0.0204 ± 0.0007 | 9-2,9-2,9-2 | 伪影严重 |
| MFAP,9 高度 × 1 角度 | **0.0056 ± 0.0010** | **9-2,9-2,9-2** | 8-5(1.23 µm) |
| HSSA,3 高度 × 3 角度 | 0.0115 ± 0.0013 | 8-4,8-2,8-1 | 8-6(1.10 µm) |

理想化仿真(1 个种子)结论相同:MFAP 0.0029(9-2),HSSA 0.0093(8-4),LISA 0.0184(7-2 到 9-2 全部达标,只有 7-1 因孪生像伪影对比度 0.088 略低于阈值,按"所有更粗单元都达标"的规则记为无)。

LISA 的所有 9 帧共用正入射帧的自动对焦距离(即几何高度),并使用已知倾角的倾斜谱核。早先一版让 LISA 每帧各自自动对焦,斜照明帧得到的是偏大的"等效距离",其中两帧还对焦失败(偏差约 −130 µm 和 +28 µm),导致 LISA 的 MSE 虚高到约 0.046。上表是修正后的数值,修正前的数值保留在提交历史中。

图:[`results/fig3_usaf_compare.png`](results/fig3_usaf_compare.png)(含第 7 组剖面)、[`results/fig_seed_summary.png`](results/fig_seed_summary.png)

### 3.3 消融与扩展(3 高度 × 3 角度数据,3 种子)

| 配置 | MSE | 最细可分辨单元 | 说明 |
|---|---|---|---|
| HSSA(论文模型) | 0.0115 ± 0.0013 | 8-4,8-2,8-1 | 基准 |
| 关闭 p 更新(γ = 0) | 0.0115 ± 0.0013 | 8-4,8-2,8-1 | 与完整 HSSA 相同,p 不起作用 |
| 模值替换改为"上采样后直接替换" | 0.0128 ± 0.0009 | 8-4,8-3,8-1 | 字面读法略差 |
| 用真实几何(z 与位移)代替自动对焦与配准 | 0.0160 ± 0.0019 | 8-5,8-3,7-3 | 更差:斜照明帧的"等效距离"比几何高度更适合正入射核 |
| 斜照明帧换成等量纯横移 | **0.0056 ± 0.0007** | 9-2,9-2,9-2 | 与 MFAP 持平:问题出在斜照明的建模 |
| **扩展:由位移反推倾角 + 倾斜谱核** | **0.0062 ± 0.0008** | 9-2,9-2,9-2 | 不需要标定即可修复 |
| 扩展,3 高度 × 2 角度(6 帧) | 0.0064 ± 0.0010 | 9-2,9-2,9-1 | 6 帧已接近 9 帧 MFAP |
| 扩展,2 高度 × 3 角度(6 帧) | 0.0077 ± 0.0012 | 9-2,9-2,9-2 | |
| LISA 数据不给角度、按 HSSA 方式用配准位移 + 每帧自动对焦 | 0.0352 ± 0.0028 | 无,7-6,无 | 对比上面的 LISA(0.0204):只有一个高度时,忽略倾角的代价很大 |

### 3.4 对应 Supplement Note 2:纯相位样品

论文用 "ship" 和 "mandrill" 图作相位,本复现用 scikit-image 自带的 "camera" 和灰度 "astronaut" 替代(相位 0 到 π,尺寸 400 µm)。与论文的仿真一样采用理想化条件:平面波、无背景、已知距离与位移,保留噪声。

| 样品 | 方法 | 相位 RMSE(rad) | 细节相位 RMSE,< 10 µm(rad) |
|---|---|---|---|
| camera | LISA | 0.899 | 0.196 |
| camera | MFAP | 0.852 | 0.174 |
| camera | HSSA | 0.857 | 0.179 |
| camera | HSSA + 倾斜谱核(扩展) | **0.846** | **0.172** |
| astronaut | LISA | 0.899 | 0.278 |
| astronaut | MFAP | 0.806 | 0.215 |
| astronaut | HSSA | 0.815 | 0.230 |
| astronaut | HSSA + 倾斜谱核(扩展) | **0.797** | **0.206** |

- 论文称 HSSA 相位误差最小。本复现中论文模型的 HSSA 略逊于 MFAP,加入倾斜谱核后最好。LISA 最差,与论文一致。
- 总 RMSE 约 0.8 rad,主要来自大面积平缓相位(如 camera 中的深色外套)没有恢复出来。这是同轴全息的固有限制:相位传递函数在低频趋于零,0.975–1.375 mm 的高度范围不足以补上。四种方法都有这个问题。"细节 RMSE"只评价 10 µm 以下的相位细节,更能区分方法。
- LISA 在两张图上的总 RMSE 几乎相同(0.8986 与 0.8986 rad,第五位才不同),这不是错误:两张图的相位标准差也几乎相同(0.934 与 0.937 rad),LISA 只有一个高度,几乎没有恢复低频相位,误差约为相位标准差的 96 %。
- 本复现没有考察论文所说的相位卷绕问题,因为相位范围设为 0 到 π。

图:[`results/figS1_phase.png`](results/figS1_phase.png)

### 3.5 照明角扫描

同一组 3 高度 × 3 角度的配置,把最大照明角设为约 2°、4°、8°(针孔横移上限 2.4、4.9、9.8 cm,下限取上限的 40 %),每个角度 3 个种子:

| 最大照明角 | HSSA(论文模型)MSE | 各种子最细单元 | HSSA + 由位移反推倾角 MSE | 各种子最细单元 |
|---|---|---|---|---|
| 2° | 0.0099(0.0079 / 0.0140 / 0.0079) | 9-1,8-6,8-5 | **0.0063**(0.0063 / 0.0075 / 0.0052) | 9-2,9-2,9-2 |
| 4° | 0.0129(0.0091 / 0.0171 / 0.0125) | 9-1,8-4,8-3 | **0.0070**(0.0061 / 0.0080 / 0.0068) | 9-2,9-2,9-2 |
| 8° | 0.0145(0.0105 / 0.0222 / 0.0108) | 8-4,8-2,8-1 | **0.0074**(0.0058 / 0.0105 / 0.0058) | 9-2,9-2,9-2 |

- 论文模型的误差随最大照明角增大而增大,与第 4 节的非傍轴离焦分析一致。扩展方法基本不随角度变化。
- 种子 2 在 1.175 mm 高度的正入射帧上自动对焦失败(偏 −110 µm)。论文流程逐帧自动对焦,这一帧的错误直接进入 HSSA,所以种子 2 的论文模型误差偏大。扩展方法用同一高度 3 帧对焦结果的中位数兜底(正入射帧偏离中位数超过 30 µm 时启用),不受影响。这也说明逐帧自动对焦是论文流程中一个实际的薄弱点。

图:[`results/fig_angle_sweep.png`](results/fig_angle_sweep.png)

## 4. 为什么斜照明在 HSSA 的模型里反而有害

HSSA 把每一帧都当作"正入射全息图 + 横向平移":前向算子是 D·A_n·T_n,其中 A_n 是正入射的角谱传播,T_n 是配准得到的平移。这个近似只在傍轴下成立。

对倾角为 θ 的平面波照明,物体频谱在传播时感受到的是 H(f + f0),f0 = sinθ/λ。把 k_z(f + f0) 在 f0 附近展开到二阶:

- 一阶项是线性相位,对应横向平移 z·tanθ。这部分被配准吸收,HSSA 处理得对。
- 二阶项是离焦。沿倾斜方向的等效距离是 z/cos³θ,垂直方向是 z/cosθ。两者都和正入射的 z 不同,而且彼此不同,相当于一个像散离焦。

在本文的参数下(θ ≤ 8°,z ≈ 1–1.4 mm):

| 量 | 数值 |
|---|---|
| 8° 时 1/cos³θ − 1 | 3.0 % |
| 8° 时 1/cosθ − 1 | 1.0 % |
| z = 1.375 mm 时两方向等效距离之差 | 约 28 µm |
| 相干焦深 λ/NA²(NA 取 0.3–0.4) | 约 3–6 µm |

自动对焦只能给每帧一个距离,落在两个方向之间(仿真里斜照明帧的对焦结果比几何高度大 5–16 µm,见 `results/usaf_preprocessing.json`),剩下的像散误差仍比焦深大几倍。于是斜照明帧给 HSSA 带来的不是新的高频信息,而是模型误差:3 个高度时,角度从 1 增到 3,MSE 不降反升。

两个消融实验把这一点隔离了出来:

- **把斜照明换成等量的纯横移**(同样的亚像素位移,正入射):HSSA 立刻达到与 MFAP 相当的水平。说明 HSSA 在 3×3 数据里真正利用到的是亚像素位移多样性,斜照明的"合成孔径"部分没有贡献,反而引入误差。
- **用配准位移反推倾角,改用倾斜谱核 H(f + f0)**(本复现的扩展,仍然不需要标定照明角):同一高度上斜照明帧相对正入射帧的位移 Δs 满足 tanθ = Δs·p/z,由此得到 f0,并把该帧的距离设为同高度正入射帧的自动对焦距离。仿真中这样估计的 tanθ 与真值只差 10⁻⁴ 量级(最大约 2 × 10⁻⁴)。改用倾斜谱核后,HSSA 的误差回落到与 MFAP 同一水平。

## 5. 照明分离变量 p 基本不起作用

Algorithm 1 中 x 的更新分母约为 (1−α)|p|² + α·max|p|²,p ≈ 1 时约等于 1,x 每次迭代几乎吸收全部修正量 φ′ − φ。p 的更新乘以 γ = 0.05,分母又以 β·max|x|² 为主,因此每次只移动残差的约 3 %,而残差在几次迭代后就很小。实测(`results/p_diagnostic.json`,真实化仿真,3×3 数据):

| γ | mean |p − 1| | max |p − 1| | p 相位标准差 |
|---|---|---|---|
| 0.05(论文值) | 0.0026 | 0.038 | 0.0013 rad |
| 0.5 | 0.023 | 0.36 | 0.012 rad |

对应地,关闭 p 更新(γ = 0,等价于在同样 9 帧上跑 MFAP 式平均投影)与完整 HSSA 的 MSE 在每个种子上相差不超过 0.0001。在本仿真里,HSSA 相对 MFAP 的差别完全来自数据组合(3 高度 × 3 角度 vs 9 高度),而不是来自照明分离。

注意 p 与 x 只以乘积 φ = p·x 进入前向模型,二者的拆分本身是不唯一的;p 能吸收什么取决于初始化和步长。论文所说的"抑制孪生像与环境噪声"在本仿真(含平滑背景照明不均匀)中没有观察到。

## 6. 本仿真的局限

- **只模拟了传感器的一个 686 µm 窗口**,而论文是 28.6 mm² 全幅。窗口内的有效探测数值孔径小于全幅,但 MFAP 与扩展方法都达到了重建网格的上限 9-2,说明窗口没有限制本文关心的 8-4 到 8-6 范围。
- **重建网格的上限是 9-3**(2 倍上采样,0.67 µm)。论文未给上采样倍数;若用更高倍数,各方法的绝对分辨率会变化,但本文的比较结论(斜照明建模误差、p 不起作用)与倍数无关。
- **轴向位置是精确的。** 仿真只加了 2 µm 横向平台抖动,没有模拟手动平台的轴向定位误差、样品倾斜或重复移动带来的漂移。MFAP 需要 9 次轴向移动,HSSA 只需 3 次,因此真实实验中 MFAP 受到的机械误差更多。论文中 HSSA 胜过 MFAP,很可能部分来自这一点,本仿真无法验证。
- **MFAP 的具体实现未知。** 本复现的 MFAP 是与 HSSA 共用框架的并行平均投影,使用同样的自动对焦和配准,这对 MFAP 是有利的设定。
- **传感器模型简化。** 100 % 填充因子、无串扰、单色。论文所用 MV-GE1600C 型号带 "C",可能是彩色传感器,论文未说明如何处理 Bayer 阵列。
- **只有 USAF 与两张相位图。** 没有模拟论文中的胃溃疡切片、蜂翅、松茎等厚样品;薄样品近似下也无法考察"相位卷绕"。
- **分辨判据是自定的**(对比度 ≥ 0.1)。论文是目视判断。因此本报告以 MSE 均值为主要依据,最细可分辨单元只作参考。

## 7. 下一步:用真实数据验证

仿真已经给出一个可以用实验检验的具体预测:在论文的装置上,斜照明帧的正入射建模误差会随照明角增大而增大,而用配准位移反推倾角的倾斜谱核可以消除它。验证需要以下数据之一:

1. **向作者索取论文数据。** 论文写明数据 "may be obtained from the authors upon reasonable request",通讯作者邮箱 zjliu@hit.edu.cn。需要 USAF 靶的 9 帧原始全息图(3 高度 × 3 角度),最好附上 MFAP 与 LISA 用的帧和各帧的针孔位置记录。
2. **自行采集。** 按表 2.1 的参数搭建,针孔横移 0、±5、±10 cm 各采若干帧,每个针孔位置在 3 个高度采集,另外单独采 9 个高度的正入射帧用于 MFAP。

拿到数据后,本仓库的流程可以直接使用:`hssa/preprocess.py` 做自动对焦与配准,`hssa/recon.py` 的 `reconstruct` 与 `estimate_tilts` 分别跑论文模型和扩展方法。需要补写的只有读取原始图像和暗场/平场处理。
